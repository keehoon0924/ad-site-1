/**
 * main.js
 * - 공통 엔트리(초기화/공용 이벤트 바인딩)
 * - 섹션별 스크립트는 assets/js/sections/*.js 형태로 분리 권장
 */

(() => {
  "use strict";

  // DOM Ready
  document.addEventListener("DOMContentLoaded", () => {
    // 공용 초기화 로직이 생기면 여기에 추가
  });
})();

